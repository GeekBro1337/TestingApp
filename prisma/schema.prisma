datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Notification {
  id          Int      @id @default(autoincrement())
  title       String
  username    String
  message     String
  scheduledAt DateTime
  userToken   String
}
